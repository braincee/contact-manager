import React from 'react';

const About = () => {
    return <div>
        <h1>About Contact Manager</h1>
        <p className='fst-italic w-50 mt-3'>This is a simple CRUD Operation Contact Manager. A user can create, read, 
            update(edit) and delete members of their contacts. I used the mainly Class components, 
            Context API to get data from an external API(JSON placeholder). 
        </p>
    </div>;
}


export default About;